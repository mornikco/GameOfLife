on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v3

    - name: setup gradle
      uses: gradle/gradle-build-action@v2.4.2
    
    - name: run tests
      run: ./gradlew build
